# Transcription is local WhisperX — no API key needed.
# Optional: a HuggingFace token enables speaker diarization (the speaker_id field).
# The token holder must also accept the pyannote diarization model's terms on HuggingFace.
# Without this, transcription still works; speaker labels are just null.
HF_TOKEN=
